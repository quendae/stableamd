# StableAMD v0.3 Text-to-SVG physical acceptance

Date: 2026-09-21

Target: Windows + AMD Radeon RX 6950 XT 16 GiB (`gfx1030`).

Status: **implementation complete / automated verification pending at current head / physical acceptance not yet performed**.

This worksheet is intentionally left unchecked until the feature is exercised on the target machine. Automated CI is not hardware acceptance.

## Setup

```text
[ ] pull the current feat/stableamd-v0.3 head
[ ] stop the old StableAMD process
[ ] start StableAMD with Start-StableAMD.cmd
[ ] hard-refresh the browser after the frontend update
[ ] Vector page is visible immediately after Generate
[ ] dependency banner reports missing/invalid/ready correctly
[ ] Install Vectorizer installs the pinned VTracer + resvg_py toolchain when needed
[ ] Generate SVG remains disabled until dependency status is ready
```

Record before testing:

```text
Branch/head:
GPU:
Driver/runtime notes:
Vector dependency status before install:
Vector dependency status after install:
VTracer version:
resvg_py version:
```

## Sample 1 — simple two-color icon

Suggested prompt: `flat red fox head icon, geometric ears and muzzle, two colors, no text`

Suggested settings:

```text
Style: Icon / Logo mark
Detail: Simple
Colors: 2
Background: Transparent
Seed: blank/random
```

Acceptance:

```text
[ ] raster generation completes
[ ] GPU runtime is released before local vectorization
[ ] VTracer completes
[ ] final SVG passes Clean SVG sanitization
[ ] PNG preview is rendered from the sanitized final SVG
[ ] transparent exterior background is correct
[ ] interior same-colored regions are not accidentally removed
[ ] result page shows SVG metadata
[ ] Gallery shows SVG badge and preview
[ ] Download SVG works
[ ] View source shows text safely, without rendering raw SVG markup into the app DOM
[ ] Reuse restores Vector controls
[ ] Delete removes SVG, preview, owned intermediates and history
```

Record:

```text
Raster generation time:
Vectorization time:
Final path count:
Final node count:
Background result:
SVG visual result:
Preview result:
Gallery reuse result:
Gallery delete result:
Notes:
```

## Sample 2 — text-free logo mark / symbol

Suggested prompt: `minimal mountain and river logo mark, balanced geometric symbol, no words, no letters`

Suggested settings:

```text
Style: Icon / Logo mark
Detail: Medium
Colors: 4
Background: Transparent
Seed: blank/random
```

Acceptance:

```text
[ ] no generated text/letters/numbers are visible
[ ] symbol remains readable at small size
[ ] edges are clean rather than photographic/soft
[ ] palette remains restrained
[ ] final SVG opens/downloads successfully
[ ] preview matches the downloaded SVG
[ ] source route returns only the persisted sanitized SVG
```

Record:

```text
Raster generation time:
Vectorization time:
Final path count:
Final node count:
Background result:
SVG visual result:
Preview result:
Gallery reuse/delete result:
Notes:
```

## Sample 3 — 4–8 color flat illustration

Suggested prompt: `flat vector illustration of a small cabin beside pine trees and a lake, clean layered shapes, no text`

Suggested settings:

```text
Style: Vector Illustration
Detail: Medium
Colors: 8
Background: Solid
Solid color: #ffffff
Seed: blank/random
```

Acceptance:

```text
[ ] composition is more complete than icon mode
[ ] solid background is preserved intentionally
[ ] palette remains within the requested visual range
[ ] no gradient/blur/raster-like debris dominates the trace
[ ] final SVG remains within sanitizer complexity limits
[ ] preview is visually consistent with final SVG
```

Record:

```text
Raster generation time:
Vectorization time:
Final path count:
Final node count:
Background result:
SVG visual result:
Preview result:
Gallery reuse/delete result:
Notes:
```

## Sample 4 — detailed vector illustration

Suggested prompt: `detailed flat vector illustration of a retro delivery van in front of a small workshop, layered clean shapes, no text`

Suggested settings:

```text
Style: Vector Illustration
Detail: Detailed
Colors: 16
Background: Transparent
Seed: blank/random
```

Acceptance:

```text
[ ] detailed preset completes without excessive local CPU/RAM growth
[ ] complexity remains under Clean SVG node/path limits
[ ] exterior background removal does not erase enclosed white/light subject details
[ ] final SVG remains editable shape geometry rather than embedded raster content
[ ] no script, image, text, external href, event handler, DTD/entity or unknown namespace appears in final SVG
```

Record:

```text
Raster generation time:
Vectorization time:
Final path count:
Final node count:
Background result:
SVG visual result:
Preview result:
Gallery reuse/delete result:
Notes:
```

## Regression after Vector tests

Text-to-SVG must not destabilize the accepted raster providers.

```text
[ ] ordinary Z-Image Turbo txt2img completes after the four Vector samples
[ ] ordinary Krea 2 Turbo txt2img completes after the four Vector samples
[ ] Gallery raster cards still expose their normal raster post-actions
[ ] Vector Gallery cards do not expose raster-only post-actions
[ ] /api/generate async behavior is unchanged
[ ] no unexpected VRAM reservation remains after Vector local tracing
[ ] StableAMD can generate another Vector item after raster regression checks
```

Record:

```text
Z-Image result:
Krea 2 result:
Post-test VRAM/runtime notes:
Any StableAMD/ComfyUI error tail:
```

## Acceptance decision

Do not mark Text-to-SVG target-accepted until all relevant checks above pass on RX 6950 XT.

```text
[ ] TEXT-TO-SVG V1 PHYSICAL ACCEPTANCE PASSED
```
